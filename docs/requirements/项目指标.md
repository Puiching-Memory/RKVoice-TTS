1. 实时性指标
基于 RK3588 平台 NPU 硬件加速，实现 端侧离线 TTS 实时合成，
单句合成延迟 ≤ 200 毫秒，满足短波电台实时语音播报需求。
2. 语音合成质量指标
采用工业级中文 TTS 模型，
中文合成准确率 ≥ 95%，
通信指令、专业词汇发音准确率 99%，
满足电台高可靠播报要求。
3. 端侧轻量化与可靠性指标
模型经 INT8 量化后 体积 ≤ 200MB，
运行内存占用 ≤ 300MB，
支持 7×24 小时不间断稳定运行，
满足嵌入式设备长期可靠工作要求。